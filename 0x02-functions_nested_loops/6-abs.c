#include "main.h"
/**
 * _abs - the absolute output value of an integer input
 * @j: to check integer input
 *
 * Return: Always j
 */


int _abs(int j)
{
	if (j >= 0)
		return (j);

	else
	{
		j *= -1;
		return (n);
	}
}
