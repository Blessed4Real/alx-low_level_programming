#include <stdio.h>

/**
 * main - print all combination fo two digits
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int b;

	i = 48;

	while (i < 58)
	{
		putchar(b);
		if (b != 57)
		{
			putchar(44);
			putchar(32);
		}
		b++;
	}

	putchar(10);

	return (0);
}
